import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Sort = () => {
  return (
    <View>
      <Text>Sort</Text>
    </View>
  );
};

export default Sort;

const styles = StyleSheet.create({});
